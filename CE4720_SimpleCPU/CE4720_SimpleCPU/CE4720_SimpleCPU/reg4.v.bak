module reg4 (
    input clk,
    input load,
    input [3:0] d,  //4 bit register
    output reg [3:0] q  //4 bit output
);
    always @(posedge clk) begin  //high edge
        if (load)
            q <= d;
    end
endmodule